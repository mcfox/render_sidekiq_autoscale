services:
  - type: cron
    name: mcfox-autoscale
    env: ruby
    plan: Starter
    schedule: "*/30 * * * *"
    buildCommand: "gem install bundler:2.3.20; bundle install"
    startCommand: "bundle exec ruby run.rb"
